const express = require('express');

const server = express();

server.listen(4242, () => {
  console.log('Express Server is running...');
});


// npm init --yes
// npm i express

//web frameworks that use node meteor, koa, sails